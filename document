Rubyでユーザーが年月日をYYYY-MM-DD形式で一度に入力し、その日付の曜日を日本語で出力するプログラムを作成してください。プログラムは以下の要件を満たす必要があります。:

1.ユーザーがYYYY-MM-DD形式で年月日を入力できる。
2.入力された年月日を年、月、日に分割して、それぞれ整数として扱う。
3.分割した年、月、日を使ってDateオブジェクトを作成し、その日の曜日を取得する。
4.取得した曜日を日本語（日、月、火、水、木、金、土）で出力する。

プログラムの例を示してください。